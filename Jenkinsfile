pipeline {
  agent any
  stages {
    stage('Init') {
      steps {
        sh 'echo "Starting..."'
      }
    }
    stage('Deploy to RC') {
      steps {
        sh '"Deploying to RC."'
      }
    }
  }
}