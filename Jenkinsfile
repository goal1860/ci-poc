pipeline {
  agent any
  stages {
    stage('Init') {
      steps {
        sh 'echo "Starting..."'
      }
    }
    stage('Deploy to RC') {
      steps {
        sh 'echo "Deploying to RC."'
      }
    }
  }
}